"use strict";

// TODO: create a component that takes in an input from a textarea box. The
// input should be numbers with a single space in between (note: they will be in
// type string when you split them, which means you'll need to convert them to
// type number before doing any calculuations. After you've successfully split
// the numbers and converted them, you can then do your statistical
// calculations.)


/**  NumbersForm: checks textarea box for input and sends valid data to be
 *  calculated for statistical purposes.
 *
 *  Props:
 *  - handleSave
 *
 *  State:
 *  - formData
 *  - validators
 *
 *  App -> NumbersForm
 */

function NumbersForm({ handleSave }) {

}
